spring:
  application:
    name: yeoun

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: soojong9627
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false

  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 500MB

server:
  port: 8080

fastapi:
  base-url: http://10.0.0.2:8000

#jwt:
#  secret: ${JWT_SECRET}
